<Window x:Class="CoreViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoreViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Analyzer selection and controls -->
        <StackPanel Orientation="Horizontal" Margin="5">
            <ComboBox x:Name="AnalyzerSelector" Width="250" DisplayMemberPath="Name"/>
            <Button Content="Run Analyzer" Margin="5,0" Click="RunAnalyzer_Click"/>
            <TextBox x:Name="FilterBox" Width="200" Margin="5,0" Text="Filter..." TextChanged="FilterBox_TextChanged"/>
        </StackPanel>

        <!-- Diagnostics display -->
        <ListView x:Name="DiagnosticsList" Grid.Row="1" ItemsSource="{Binding DiagnosticsView}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Analyzer" Width="120" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Severity" Width="70" DisplayMemberBinding="{Binding Severity}"/>
                    <GridViewColumn Header="Impact" Width="80" DisplayMemberBinding="{Binding Impact}"/>
                    <GridViewColumn Header="File" Width="250" DisplayMemberBinding="{Binding FilePath}"/>
                    <GridViewColumn Header="Line" Width="50" DisplayMemberBinding="{Binding LineNumber}"/>
                    <GridViewColumn Header="Message" 
                                    DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
