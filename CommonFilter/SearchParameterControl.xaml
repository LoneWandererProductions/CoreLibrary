<UserControl x:Class="CommonFilter.SearchParameterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="800" x:ClassModifier="internal">

    <Grid Margin="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ComboBox Grid.Column="0" ItemsSource="{Binding LogicalOperatorOptions}"
                  SelectedItem="{Binding SelectedLogicalOperator}" Margin="0,0,5,0" Width="90" />
        <ComboBox Grid.Column="1" ItemsSource="{Binding OperatorOptions}"
                  SelectedItem="{Binding SelectedCompareOperator}" Margin="0,0,5,0" Width="110" />
        <TextBox Grid.Column="2" Text="{Binding EntryText}" Margin="0,0,5,0" VerticalContentAlignment="Center" />
        <Button Content="Delete" Grid.Column="3" Command="{Binding DeleteCommand}" Padding="10,2" />
    </Grid>
</UserControl>